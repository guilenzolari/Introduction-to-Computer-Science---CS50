// imprime a palavra meow n vezes
#include <stdio.h>

int main(void)
{
    int i = 0;    
    while (i < 3)
    {
        printf("meow\n");
        i++;
    }
}

// (i = i + 1) é o mesmo que (i++) e (i+=1)
//(i = i -1) é o mesmo que (i--) e (i-=1)